import {Meta} from '@storybook/react';
import {RadioButtonStories} from './components/RadioButton.stories';

const meta = {
    component: RadioButtonStories,
    title: 'Radio Button',
} satisfies Meta<typeof RadioButtonStories>;

export default meta;
