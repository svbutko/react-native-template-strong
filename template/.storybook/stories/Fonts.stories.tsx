import {Meta} from '@storybook/react';
import {FontsList} from '../components/FontsList';

const meta = {
    component: FontsList,
    title: 'Fonts',
} satisfies Meta<typeof FontsList>;

export default meta;
