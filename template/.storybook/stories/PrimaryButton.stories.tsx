import {Meta} from '@storybook/react';
import {PrimaryButtonStories} from './components/PrimaryButton.stories';

const meta = {
    component: PrimaryButtonStories,
    title: 'Primary Button',
} satisfies Meta<typeof PrimaryButtonStories>;

export default meta;
