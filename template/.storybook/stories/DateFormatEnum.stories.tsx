import {Meta} from '@storybook/react';
import {DateFormatList} from '../components/DateFormatList';

const meta = {
    component: DateFormatList,
    title: 'Date Formats Enum',
} satisfies Meta<typeof DateFormatList>;

export default meta;
