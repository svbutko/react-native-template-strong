import {Meta} from '@storybook/react';
import {EmptyViewStories} from './components/EmptyView.stories';

const meta = {
    component: EmptyViewStories,
    title: 'Empty View',
} satisfies Meta<typeof EmptyViewStories>;

export default meta;
