import {Meta} from '@storybook/react';
import {AlertsStories} from './components/Alerts.stories';

const meta = {
    component: AlertsStories,
    title: 'Alerts and dialogs',
} satisfies Meta<typeof AlertsStories>;

export default meta;
