import {Meta} from '@storybook/react';
import {DatePickerOverlayStories} from './components/DatePickerOverlay.stories';

const meta = {
    component: DatePickerOverlayStories,
    title: 'Date Picker',
} satisfies Meta<typeof DatePickerOverlayStories>;

export default meta;
