import {Meta} from '@storybook/react';
import {PrimaryTextInputStories} from './components/PrimaryTextInput.stories';

const meta = {
    component: PrimaryTextInputStories,
    title: 'Primary Text Input',
} satisfies Meta<typeof PrimaryTextInputStories>;

export default meta;
